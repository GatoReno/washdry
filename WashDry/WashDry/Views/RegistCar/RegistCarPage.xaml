<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:colorPicker="clr-namespace:Spillman.Xamarin.Forms.ColorPicker;assembly=Spillman.Xamarin.Forms.ColorPicker"
             mc:Ignorable="d"
             x:Class="WashDry.Views.RegistCar.RegistCarPage">

    <ContentPage.BindingContext>
        <colorPicker:ColorPickerViewModel />
    </ContentPage.BindingContext>

    <ContentView  
                 Padding="8"
                 >
        <ScrollView>
            <StackLayout Margin="10,10,10,10">

                <ActivityIndicator
                    x:Name="Cator"
                    IsVisible="False"
                    IsRunning="False"/>

                <Label
                    IsVisible="true"
                    Text="Agregar tu auto"
                    HorizontalOptions="Center"
                    x:Name="lblError"></Label>
                <Label
                
                    Text="10101"
                    IsVisible="false"
                    x:Name="iduser"></Label>
 

                <Label Text="Marca" x:Name="lblmarca"></Label>
                <Entry x:Name="Marca_" />


                <Label Text="Modelo" x:Name="lblmodelo"></Label>
                <Entry x:Name="Modelo" />

                <Label Text="Año" x:Name="lblamio"></Label>
                <DatePicker
                x:Name="Ann_"
                Format="yyyy"
                />

                <Label x:Name="lblPlacas" Text="Placas"></Label>
                <Entry x:Name="Placas" />

     

           


                <Button x:Name="btnCamara"
                    Text="Tomar foto"
             
              Clicked="btnCamara_Clicked"
                     />

                <Button x:Name="btnGal"
                    Text="Galeria "
                    Clicked="btnGal_Clicked"
                     />


                <Image
                    x:Name="imgx"
                    WidthRequest="200"
                   
                    />

                <colorPicker:ColorPickerView  x:Name="colorx"/>

                <Button
                    Text="Agregar"
                    x:Name="AgregarAutobtn"
                   
                    Clicked="AgregarAutobtn_Clicked"
                    />
            </StackLayout>

        </ScrollView>


    </ContentView>
</ContentPage>